- if user_signed_in?
  - if @user.followed?(current_user)
    = link_to 'フォローを解除', user_follow_path(@user), method: :delete, remote: true
  - else
    = link_to 'フォローする', user_follow_path(@user), method: :post, remote: true